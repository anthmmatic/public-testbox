on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: |
          for i in {1..24}; do
            echo "::error title=Error Title::annotation ${i}"
          done
