on:
  workflow_dispatch:
  workflow_call:
    inputs:
      id:
        required: false
        type: string

jobs:
  job-name:
    name: First-text / Second-text
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
