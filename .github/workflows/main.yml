on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          SECRET_2="first%25secondthird"
          echo "::add-mask::$SECRET_2"
          echo $SECRET_2
