on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: write

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
      - run: |
          echo 'done'
