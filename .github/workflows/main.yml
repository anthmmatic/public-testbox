on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    container:
      image: alpine
      options: |
        -e "MYENV=value"
        -e "MYENVTWO=${{secrets.MYSEC}}"
    steps:
      - run: |
          echo "${MYENV}"
          echo "${MYENVTWO}"
