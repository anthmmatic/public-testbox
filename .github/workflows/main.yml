on:
  workflow_dispatch:

jobs:
  job-one:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
  job-two:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        id: [1,2,3,4,5]
    steps:
      - run: echo "HERE"
