on:
  workflow_dispatch:

jobs:
  job-one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "content in this file" > file.txt
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
