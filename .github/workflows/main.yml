on:
  workflow_dispatch:
    inputs:
      env:
        description: github env to deploy to
        required: false
        type: choice
        default: ''
        options:
          - ''
          - dev
          - test
          - staging
          - production

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
