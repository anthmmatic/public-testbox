on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - name: OS packages
        run: |
          sudo apt-get update
          sudo apt-get install -yqq \
            gdal-bin \
            libpq-dev
