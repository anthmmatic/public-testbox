on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          SECRET_1=$( echo "%3Daval%252F20231011%2Fus-east-1" | base64 )
          echo $SECRET_1
          echo "::add-mask::$SECRET_1"
          echo $SECRET_1
          echo "%3Daval%252F20231011%2Fus-east-1"

          SECRET_2="%25dwdwefw"
          echo "::add-mask::$SECRET_2"
          echo $SECRET_2
