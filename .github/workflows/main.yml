on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "first"
      - if: false
        run: echo "skip"
      - run: sleep 1m
      - if: always() && success()
        run: echo "ran anyway"
