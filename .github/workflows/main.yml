on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ 'ubuntu', 'windows', 'macos' ]
    runs-on: '${{ matrix.os }}-latest'
    steps:
      - run: ls /etc
      - run: ls ~/etc
      - run: cat /etc/ssh_config
