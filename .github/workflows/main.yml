on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Change things
        run: echo "foo" >> README.md
      - name: Commit and push changes
        run: |
          git config --global user.email "abcabcabcabc@abc"
          git config --global user.name "nonsense-name"
          git add .
          git commit -S -m "Update test file"
          git push
