on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ 'ubuntu', 'windows', 'macos' ]
    runs-on: '${{ matrix.os }}-latest'
    steps:
      - run: cat /etc/ssh/ssh_config
        shell: bash
