on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          VALUE="%3Dfirst%25secondthird"
          
          echo "::add-mask::${VALUE}"
          echo "${VALUE}"

          VALUE_ENCODED=$( echo "$VALUE" | base64 )
          echo "::add-mask::$VALUE_ENCODED"
          echo "$VALUE_ENCODED"
          echo "$VALUE_ENCODED" | base64 -d
