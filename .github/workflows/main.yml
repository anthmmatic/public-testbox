on:
  workflow_dispatch:

jobs:
  job-one:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Done"
  job-two:
    needs: job-one
    runs-on: ubuntu-latest
    steps:
      - run: echo "Done"
  job-three:
    needs: job-one
    runs-on: ubuntu-latest
    steps:
      - run: echo "Done"
  job-result:
    if: ${{ !success() }}
    needs: [job-two, job-three]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Done"
