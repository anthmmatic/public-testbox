on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ::warning title="Warning Title"::"link- https://github.com https://www.google.com"
      - run: |
          echo "link- https://github.com https://www.google.com" >> $GITHUB_STEP_SUMMARY
