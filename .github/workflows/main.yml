on:
  pull_request:

permissions:
  pull-requests: write
  contents: read

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          PR_NUMBER=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")
          gh pr review $PR_NUMBER --comment -b "interesting"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
