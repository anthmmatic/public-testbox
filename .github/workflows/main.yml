on:
  workflow_dispatch:

jobs:
  prerequisite:
    name: Prerequisite
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo failure
          exit 1
  required:
      name: Required
      runs-on: ubuntu-latest
      needs: prerequisite
      steps:
        - run: echo success
    
