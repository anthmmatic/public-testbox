on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "first"
      - run: sleep 1m
      - if: false
        run: echo "skip"
      - if: success() || cancelled()
        run: echo "ran anyway"
