on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          SECRET_2="%3Daval%2520231011%2Fus-east-1"
          echo "::add-mask::$SECRET_2"
          echo $SECRET_2
