on:
  workflow_dispatch:
    inputs:
      num:
        description: number input
        required: false
        type: number
      str:
        description: string input
        required: false
        type: string

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
