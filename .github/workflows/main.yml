on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y qemu-system-arm


  test:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y qemu-system-arm --fix-missing
