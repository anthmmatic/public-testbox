on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "first"
      - run: sleep 30s
      - if: false
        run: echo "skip"
      - if: success() || !failure() && cancelled()
        run: |
          sleep 1m
          echo "ran anyway"
