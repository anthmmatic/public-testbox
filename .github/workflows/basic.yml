on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-one:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
  job-two:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
  job-three:
    needs: [job-one, job-two, job-one]
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
