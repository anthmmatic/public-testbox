on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: |
          [ "$GITHUB_RUN_ATTEMPT" != 1 ] || exit 1
  job-name-two:
    runs-on: ubuntu-latest
    needs: job-name
    steps:
      - run: echo 'done'
