on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          SECRET_2="%3Daval%2F20231011%2Fus-east-1"
          echo "::add-mask::$SECRET_2"
          echo $SECRET_2

          SECRET_3="url%3Daval"
          echo "::add-mask::$SECRET_3"
          echo $SECRET_3

          SECRET_4="%3D%252F"
          echo "::add-mask::$SECRET_4"
          echo $SECRET_4

          SECRET_5="%3Daval%2F20231011%2Fus-east-1"
          echo "::add-mask::$SECRET_5"
          echo $SECRET_5
