on:
  workflow_dispatch:

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
  follow:
    needs: init
    if: cancelled() || success() || failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "Still running!"
    
