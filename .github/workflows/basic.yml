on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "First link: https://github.com/anthmmatic/public-testbox/actions/runs/17048963578, Second link: https://google.com/" >> $GITHUB_STEP_SUMMARY
