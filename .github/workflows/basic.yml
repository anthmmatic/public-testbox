on:
  #push:
  pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: |
          for i in {1..3000}; do
            echo "Number: $i"
          done
          exit 1
