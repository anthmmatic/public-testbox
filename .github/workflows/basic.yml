on:
  workflow_dispatch:

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v2.0.0
      - run: exit 1
  follow:
    needs: init
    if: cancelled() || success() || failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "Still running!"
    
