on:
  workflow_dispatch:

jobs:
  base-reuse:
    uses: anthms/public-actions-depot/.github/workflows/reusable.yml@main
  fork-reuse:
    uses: anthmmatic/public-actions-depot/.github/workflows/reusable.yml@v1
