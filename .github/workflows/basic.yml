on:
  workflow_dispatch:

jobs:
  base:
    runs-on: ubuntu-latest
    steps:
      - name: Testing add-mask
        run: |
          SECRET_1="a-secret"
          echo "::add-mask::$SECRET_1"
          echo $SECRET_1

          SECRET_5="%3D${SECRET_1}%252F20231011%252Fus-east-1"
          echo "::add-mask::$SECRET_5"
          echo $SECRET_5

          SECRET_6="%3D${SECRET_1}%"
          echo "::add-mask::$SECRET_6"
          echo $SECRET_6

          SECRET_7="%3D${SECRET_1}%2011%"
          echo "::add-mask::$SECRET_7"
          echo $SECRET_7

          SECRET_8="%3D${SECRET_1}%252F20231011%252Fus-east-1%"
          echo "::add-mask::$SECRET_8"
          echo $SECRET_8
