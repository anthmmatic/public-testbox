on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: macos-latest
    steps:
      - run: echo 'step 1'
      - run: echo 'step 2'
      - uses: mxschmitt/action-tmate@v3
        timeout-minutes: 15
        with:
          detached: true
