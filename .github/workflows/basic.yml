on:
  workflow_dispatch:

jobs:
  job-one:
    runs-on: ubuntu-latest
    outputs:
      release_number: ${{ steps.set.outputs.rnum }}
    steps:
      - id: set
        run: |
          echo "rnum=$RELEASE_NUMBER" >> $GITHUB_ENV
        env:
          RELEASE_NUMBER: 1628
