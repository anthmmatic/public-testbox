on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ::notice title=Notice Title::"First link: https://www.github.com , Second link: https://www.google.com"
