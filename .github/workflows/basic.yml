on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: macos-latest
    steps:
      - run: echo 'step 1'
      - run: |
          cd /
          cd /Users/runner
          pwd
          ls -la
