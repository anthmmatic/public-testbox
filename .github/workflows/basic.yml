on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: touch file.ts file.tsx
      - run: echo "content" > filetwo.tsx
      - run: ls
      - run: echo "${{ hashFiles('file.tsx?') }}" # empty output
      - run: echo "${{ hashFiles('*.ts', '*.tsx') }}" # correct hash
