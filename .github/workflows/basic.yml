on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
  job-name-two:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
