on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo "content for this file" > myfile.txt
      - uses: actions/cache@v3
        with:
          path: myfile.txt
          key: for-my-file-postsave2
      - run: cat myfile.txt
