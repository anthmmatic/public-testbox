on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo "content for this file" > myfile.txt
      - uses: actions/cache@v4
        with:
          path: myfile.txt
          key: for-my-file-postsave3
      - run: cat myfile.txt
