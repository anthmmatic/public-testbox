on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: macos-latest
    steps:
      - run: brew update
      
  job-two:
    runs-on: macos-latest
    env:
      HOMEBREW_NO_INSTALL_FROM_API: ""
    steps:
      - run: brew update
