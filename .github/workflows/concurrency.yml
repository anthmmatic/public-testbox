on:
  workflow_dispatch:

jobs:
  job1:
    concurrency:
      group: ${{ github.ref != 'refs/heads/main' && format('{0}-{1}', github.workflow, github.ref) || '' }}
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30

  job2:
    concurrency:
      group: ${{ github.ref != 'refs/heads/main' && format('{0}-{1}', github.workflow, github.ref) || '' }}
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30
