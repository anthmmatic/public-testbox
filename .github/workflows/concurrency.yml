on:
  workflow_dispatch:

jobs:
  job1:
    concurrency: ${{ github.ref != 'refs/heads/main' && github.run_id || github.job }}
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30

  job2:
    concurrency: ${{ github.ref != 'refs/heads/main' && github.run_id || github.job }}
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30
